람다식

메서드 참조: 메서드를 참조해서 매개변수의 정보와 리턴타입을 알아내겠다.
           람다식에서 불필요한 매개변수를 제거하는 것을 목적으로 한다.

           (left, right) -> Math.max(left, right);
           Math::max;

정적메서드와 인스턴스 메서드 참조
정적메서드 참조시  :: 기호를 붙이고 정적 메서드 이름을 기술한다.


매개변수의 메서드 참조

(a,b) -> {a.instanceMethod(b)}
a는 참조값, b는 a참조변수 메서드의 매개변수!

클래스 :: instanceMethod

String의 인스턴스 메서드


    생서자 참조
    생성자를 참조하다는 의미는 객체를 생성한다와 같다.
    (a,b) -> return new 클래스(a,b);
    생성자 참조 표현식
    클래스 :: new









